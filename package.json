{
  "name": "cdn-freelancer-fullstack",
  "scripts": {
    "backend": "cd src/WebAPI && dotnet run",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "frontend": "cd frontend && npm run dev",
    "server": "nodemon app.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}